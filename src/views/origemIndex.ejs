<%- include('includes/head')%> <%- include('includes/nav')%>

<div class="container">
  <div class="row">
    <div class="col-lg-2"></div>

    <div class="my-3">
      <h1 class="text-center">Origens Cadastradas</h1>
      <p class="text-center lead">Origens cadastradas no sistema</p>

      <a
        href="/origem"
        type="button"
        class="align-self-center btn btn-outline-primary mt-5"
        >Cadastrar nova origem</a
      >

      <%- include('includes/messages')%>

      <table
        id="tabela"
        class="table table-striped table-hover rounded shadow-lg"
        style="width: 100%"
      >
        <thead>
          <tr>
            <th>Origens</th>
            <th>Ações</th>
          </tr>
        </thead>
        <tbody>
          <% origens.forEach(origem => { %>
          <tr>
            <td><%= origem.origem %></td>
            <td><a href="/origem/<%= origem._id%>">Editar</a></td>
          </tr>
          <% }); %>
        </tbody>
      </table>

      <%- include('includes/footer')%>
    </div>
  </div>
</div>
